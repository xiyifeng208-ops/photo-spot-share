<view class="page">
  <view class="title">{{title}}</view>
  <view class="updated muted">最近更新：见文末生效日期</view>
  <view class="card">
    <view class="paragraph" wx:for="{{paragraphs}}" wx:key="index">{{item}}</view>
  </view>
</view>

