# 默认按文本处理，入库统一 LF
* text=auto

# Windows 批处理必须是 CRLF：cmd.exe 解析 LF 结尾的 .cmd/.bat 会跳过命令
*.cmd text eol=crlf
*.bat text eol=crlf

# Shell 脚本保持 LF，避免在 Linux/macOS 上出现 \r 报错
*.sh text eol=lf

# 二进制资源不要做换行转换
*.png binary
*.jpg binary
*.jpeg binary
*.ico binary
